pluginManagement {
    repositories {
        //gradlePluginPortal()
        maven {
            name = "ajoberstar-backup"
            url = "https://ajoberstar.github.io/bintray-backup/"
        }
    }
}
